const GlobalCssComponent = () => {
  return (
    <div className="fontSize_60">
      Global Css we use directly just need to define in globals.css file inside
      app folder
    </div>
  );
};

export default GlobalCssComponent;
